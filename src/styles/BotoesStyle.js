import StyleSheet from 'react-native'

const botao_comecar = StyleSheet.create({
  botao_comecar: {
    backgroundColor: "#343E3E",
    borderRadius: 10,
    paddingVertical: 12,
    paddingHorizontal: 24,
    alignItems: "center",
  },
  texto: {
    color: "white",
    fontSize: 16,
    fontWeight: "bold",
  },
});

export default {botao_comecar}